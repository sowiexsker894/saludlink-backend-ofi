spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5433/saludlink_db
    username: postgres
    password: admin123
  jpa:
    show-sql: true

saludlink:
  cors:
    allowed-origins: http://localhost:4200,http://localhost:5173

server:
  port: 8080

jwt:
  secret: local-dev-secret-change-in-production-min-32-chars
  expiration: 86400000
